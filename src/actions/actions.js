

{type: 'game/gameCreate', payload: images, }
{type: 'game/gameUpdate', payload:  }

{type: 'game/gameCreate', payload: }

{type: 'images/imagesLoaded', payload: images}

// get images/ assign images --> backend()
// component did mount, fetch images on backend side(fetch in index in rails, rest client, assign images to teams)
// return this to front end

// create game (has many images)
//  nested attribute [create images(attributes),]
// 